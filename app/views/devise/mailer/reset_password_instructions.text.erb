Someone has requested a link to change your password.  You can do this below:

    <%= edit_password_url(@resource, reset_password_token: @resource.reset_password_token) %>

If you didn't request this password change, please ignore this email.  Your password won't change.
