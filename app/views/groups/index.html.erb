<h1>Listing groups</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
    <td><%= group.description %></td>
    <td><%= link_to 'Show', [@organization, group] %></td>
    <td><%= link_to 'Edit', edit_organization_group_path(@organization, group) %></td>
    <td><%= link_to 'Destroy', [@organization, group], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group', new_organization_group_path(@organization) %>
